# Snapshots de CoinMarketCap (cmc-skill-hub) escritos por la skill /cmc-snapshot.
# Los archivos data/cmc/<YYYY-MM-DD>.json son artefactos runtime e ignorados por
# git (ver .gitignore). Este .gitkeep mantiene el directorio en el repo.
